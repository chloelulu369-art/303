<script>
    var letras = ['T', 'R', 'W', 'A', 'G', 'M', 'Y', 'F', 'P', 'D', 'X', 'B', 'N', 'J', 'Z', 'S', 'Q', 'V', 'H', 'L', 'C', 'K', 'E', 'T'];
    var numero;
    numero = prompt("Ingrese un número de DNI");
    if(isNaN(numero) || numero==null || numero==""){
        document.writeln("Valor inválido en la posición: ", i, "° <br>")}
 
    if(numero =< 0 || numero =< 99999999){
        //calcula la letra que le corresponde según el método explicado anteriormente.
    }

    for(let j=0; j=23 ; j++)
    {
        if(nf == letras(j)){
        document.writeln("Correcto, siga avanzando")}
        else{
        document.writeln("error,reintente") } 
    }
</script>
